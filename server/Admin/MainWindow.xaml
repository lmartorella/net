<Window x:Class="Lucky.Home.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:home="clr-namespace:Lucky.Home"
        Title="Admin UI" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Connection.StatusText}" HorizontalAlignment="Center"/>
            <TextBlock Grid.Column="1" HorizontalAlignment="Right">
                <Hyperlink Click="RefreshClicked">Refresh</Hyperlink>
            </TextBlock>
        </Grid>

        <TabControl Grid.Row="1">
            <TabItem Header="Topological View">
                <home:TopologicalView Nodes="{Binding Connection.Nodes}" TcpConnection="{Binding Connection}"/>
            </TabItem>
            <TabItem Header="Devices">
                <home:DeviceView Devices="{Binding Connection.Devices}" TcpConnection="{Binding Connection}"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
